export type Project = {
  image: string;
  slug: string;
  categoryKey: string;
  imageFolder: string;
  gallery: string[];
};

export type Stat = {
  label: string;
  value: string;
};

export type Service = {
  title: string;
  description: string;
};
